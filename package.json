{
  "name": "tredin-platform",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["apps/user", "apps/admin", "backend"],
  "scripts": {
    "build": "npm run build --workspace apps/user && npm run build --workspace apps/admin",
    "build:user": "npm run build --workspace apps/user",
    "build:admin": "npm run build --workspace apps/admin",
    "check": "npm run typecheck --workspace apps/user && npm run typecheck --workspace apps/admin",
    "start": "npm start --workspace backend",
    "preflight": "node scripts/deployment-preflight.js"
  }
}
